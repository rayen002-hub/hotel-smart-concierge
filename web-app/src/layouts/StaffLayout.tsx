// StaffLayout — delegates to DashboardLayout.
// The AuthGuard wrapping happens in routes/index.tsx; this file now simply
// renders the sidebar+topbar shell via DashboardLayout.
export { DashboardLayout as StaffLayout } from './DashboardLayout';
